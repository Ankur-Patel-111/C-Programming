// array index delete in only one array.
#include<stdio.h>
void main()
{
int a[5]={10,20,30,40,50},i,ele,j,index;
ele=sizeof(a)/sizeof(a[0]);
//printf("%ld\n",sizeof(a));
for(i=0;i<5;i++)
{
printf("%d\n",a[i]);
}
printf("Enter the index you want to delete.\n");
scanf("%d",&index);

for(i=0;i<5;i++)
{
if(i==index)
{
for(    )

}

}
}
